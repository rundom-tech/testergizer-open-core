{
  "$id": "https://testergizer.dev/schemas/results.schema.json",
  "type": "object",
  "required": [
    "schemaVersion",
    "runId",
    "suiteId",
    "executionEngine",
    "executionIntent",
    "validationMode",
    "startedAt",
    "endedAt",
    "durationMs",
    "tests",
    "summary",
    "suiteStatus",
    "signalStrength"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "type": "string", "const": "v1" },
    "runId": { "type": "string" },
    "suiteId": { "type": "string" },
    "suiteName": { "type": "string" },
    "suitePath": { "type": "string" },
    "executionEngine": { "type": "string", "enum": ["testergizer", "playwright", "api"] },
    "executionIntent": { "type": "string", "enum": ["review", "verify", "baseline"] },
    "validationMode": { "type": "string", "enum": ["debug", "strict"] },
    "startedAt": { "type": "string", "format": "date-time" },
    "endedAt": { "type": "string", "format": "date-time" },
    "durationMs": { "type": "integer", "minimum": 0 },
    "tests": { "type": "array", "items": { "$ref": "#/$defs/testResult" } },
    "summary": { "$ref": "#/$defs/runSummary" },
    "suiteStatus": { "type": "string", "enum": ["valid", "invalid", "passed", "failed"] },
    "signalStrength": { "type": "number", "minimum": -100, "maximum": 100 },
    
  },
  "$defs": {
    "testResult": {
      "type": "object",
      "required": [
        "id",
        "testDomain",
        "status",
        "startedAt",
        "endedAt",
        "durationMs",
        "steps"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "testDomain": { "type": "string", "enum": ["ui", "api", "system"] },
        "status": { "type": "string", "enum": ["passed", "failed", "skipped"] },
        "startedAt": { "type": "string", "format": "date-time" },
        "endedAt": { "type": "string", "format": "date-time" },
        "durationMs": { "type": "integer", "minimum": 0 },
        "steps": { "type": "array", "items": { "$ref": "#/$defs/stepResult" } }
      }
    },
    "stepResult": {
      "type": "object",
      "required": [
        "id",
        "action",
        "status",
        "attempts",
        "errors",
        "startedAt",
        "endedAt",
        "durationMs"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "action": { "type": "string" },
        "status": { "type": "string", "enum": ["passed", "failed", "skipped"] },
        "attempts": { "type": "integer", "minimum": 1 },
        "errors": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["reason", "message"],
            "additionalProperties": false,
            "properties": {
              "reason": { "type": "string" },
              "message": { "type": "string" },
              "stack": { "type": "string" }
            }
          }
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["code", "message"],
            "additionalProperties": false,
            "properties": {
              "code": { "type": "string" },
              "message": { "type": "string" }
            }
          }
        },
        "startedAt": { "type": "string", "format": "date-time" },
        "endedAt": { "type": "string", "format": "date-time" },
        "durationMs": { "type": "integer", "minimum": 0 }
      }
    },
    "runSummary": {
      "type": "object",
      "required": ["total", "passed", "failed", "skipped"],
      "additionalProperties": false,
      "properties": {
        "total": { "type": "integer", "minimum": 0 },
        "passed": { "type": "integer", "minimum": 0 },
        "failed": { "type": "integer", "minimum": 0 },
        "skipped": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
